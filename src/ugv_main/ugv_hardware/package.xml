<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>ugv_hardware</name>
  <version>0.0.0</version>
  <description>
    ros2_control SystemInterface for the UGV. Owns the ESP32 UART (sole reader/writer),
    exposes the drive wheels (velocity command, position/velocity state) and the on-board
    IMU as standard ros2_control interfaces, and bridges the pan-tilt / LED / magnetometer /
    voltage telemetry that share the same serial link.
  </description>
  <maintainer email="travisharty@gmail.com">Travis Harty</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
